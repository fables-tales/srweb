//TITLE:	Joining
//DESCRIPTION:	
//KEYWORDS:	
//CONTENT_TYPE:	php

<?php
// Form definition, validation and processing
// Relative to 'classes'
include("subscribe_form.php");
?>

<h1>Joining</h1>

<p>If you would like to express interest in taking part in the competition, please
do [get in contact with us](/about/contactus).</p>

<p>Unfortunately, this year's competition has now started &mdash; it is now too late to register your interest for SR 2011. Please do let us know that you'd like to take part in SR 2012, though.</p>

<h2>Subscribe to SR2012</h2>

<?php

if ($form->bound && $form->is_valid()) {
	echo "<p>Thanks for subscribing to SR2012, we aim to contact you... SOME STUFF HERE</p>";
} else {
	if ($form->has_errors())
		echo '<p class="validation-error-msg">Please correct the following errors.</p>';
	echo $form->open($_SERVER['REQUEST_URI']);

	foreach($form->fields() as $name => $field) {
		echo $field->label();
		echo $field->html();
		echo $field->errors();
	}

	echo '<input type="submit" value="Submit" />';
	echo $form->close();
}

?>

